apiVersion: 1

providers:
  - name: agenttrace
    orgId: 1
    folder: AgentTrace
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
